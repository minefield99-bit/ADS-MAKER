<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme; Compose drives the real colors. Splash/window background only. -->
    <style name="Theme.AdsMaker" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/brand_background</item>
        <item name="android:navigationBarColor">@color/brand_background</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>
</resources>
